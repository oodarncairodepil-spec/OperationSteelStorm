{
  "name": "operation-steelstorm-cloudflare-signaling",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types --config wrangler.jsonc",
    "typecheck": "npm run types && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.118.0"
  }
}
